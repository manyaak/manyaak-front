interface SelectDateStepProps {
  onMoveNextStep: () => void;
}

const SelectDateStep = ({ onMoveNextStep }: SelectDateStepProps) => (
  <div>날짜 선택 단계</div>
  // TODO: Footer) if select, activate
);

export default SelectDateStep;
